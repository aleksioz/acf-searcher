name: Deploy to Server hotmess

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Deploy via SSH
        uses: appleboy/ssh-action@v0.1.5
        with:
          host: 88.198.1.66
          port: 22222
          username: alexa
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd cd public_html
            git reset --hard
            git pull origin main
            # Restart services if needed